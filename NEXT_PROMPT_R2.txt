/api/search 还有两块需要补一下。先是分页这块:SearchOptions 里声明了 page 字段(默认 1)但 service.py 里完全没用,只用 cursor,导致 page=1 和 page=999 返的结果一模一样——前端 axios 没传 page 所以日常不影响,但 API 文档(以及任何直接 curl 调用的脚本)暗示 page 是有效的,客户端拉不到预期的第二页就以为是后端坏了。/api/stats/top-files 这边 label_map 拿的是当前索引里所有 file 的 name,索引里如果某条 file_id 在 stats 里有点击记录但文件本身已经从磁盘删了、索引清掉,label_map 里就找不到这条 file_id,top_files 直接把 file_id 本身当 label 返给前端,UI 上看到一串 hash(17b3a23dad654c4b07585d07)不像文件名,运营排查时就以为是 bug。这两个不算 R2 修过的核心问题(分页重复/路径泄漏/NOT/sanitize)的回归,但同属 search 服务的隐式 bug 漏检,顺手修一下比较好。检查修复一下。
